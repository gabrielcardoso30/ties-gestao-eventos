import type { ListarEventosItemResponse } from '@/shared/api/types'
import { ResourceListPage } from '@/shared/components/generic/ResourceListPage'
export function ListarEventosPage() { return <ResourceListPage<ListarEventosItemResponse> modulo="Eventos" titulo="Eventos" descricao="Ciclo de vida, publicação e inscrições." endpoint="/eventos" queryKey="eventos" criarEm="/eventos/novo" detalheEm={x => `/eventos/${x.id}`} columns={[{ titulo: 'Evento', render: x => <strong>{x.eventoNome}</strong> }, { titulo: 'Início', render: x => new Date(x.eventoDataInicio).toLocaleString('pt-BR') }, { titulo: 'Formato', render: x => x.eventoFormato }, { titulo: 'Situação', render: x => x.eventoSituacao }, { titulo: 'Inscritos', render: x => x.inscricoesConfirmadas }]} /> }
