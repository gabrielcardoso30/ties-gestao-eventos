import { useParams } from 'react-router-dom'; import { EntityFormPage } from '@/shared/components/generic/EntityFormPage'; import { eventoFields, transformarEvento } from '../criar/CriarEventoPage'
const local = (v: unknown) => v ? new Date(String(v)).toISOString().slice(0, 16) : ''
export function AtualizarEventoPage() { const { id } = useParams(); return <EntityFormPage titulo="Editar evento" descricao="Eventos encerrados ou cancelados não podem ser alterados." voltarPara={`/eventos/${id}`} endpoint={`/eventos/${id}`} carregarDe={`/eventos/${id}`} mapearCarregado={d => ({ ...d, eventoDataInicio: local(d.eventoDataInicio), eventoDataFim: local(d.eventoDataFim) })} method="put" fields={eventoFields} transformar={transformarEvento} invalidar={['eventos']} aoSalvar={() => `/eventos/${id}`} /> }
