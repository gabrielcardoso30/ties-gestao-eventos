import type { ListarUsuariosItemResponse } from '@/shared/api/types'
import { ResourceListPage } from '@/shared/components/generic/ResourceListPage'
export function ListarUsuariosPage() { return <ResourceListPage<ListarUsuariosItemResponse> modulo="Identidade" titulo="Usuários" descricao="Acesso, perfis e autorização da plataforma." endpoint="/identidade/usuarios" queryKey="usuarios" columns={[{ titulo: 'Nome', render: x => <strong>{x.usuarioNome}</strong> }, { titulo: 'E-mail', render: x => x.usuarioEmail }, { titulo: 'Perfis', render: x => x.perfis.join(', ') }, { titulo: 'Situação', render: x => x.estaAtivo ? 'Ativo' : 'Inativo' }]} /> }
