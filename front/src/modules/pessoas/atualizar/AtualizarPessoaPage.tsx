import { useParams } from 'react-router-dom'; import { EntityFormPage } from '@/shared/components/generic/EntityFormPage'; import { pessoaFields } from '../criar/CriarPessoaPage'
export function AtualizarPessoaPage() { const { id } = useParams(); return <EntityFormPage titulo="Editar pessoa" descricao="Atualize os dados cadastrais." voltarPara={`/pessoas/${id}`} endpoint={`/pessoas/${id}`} carregarDe={`/pessoas/${id}`} method="put" fields={[...pessoaFields, { name: 'estaAtivo', label: 'Pessoa ativa', type: 'checkbox' }]} transformar={v => ({ ...v, pessoaDocumento: v.pessoaDocumento || null })} invalidar={['pessoas']} aoSalvar={() => `/pessoas/${id}`} /> }
