import { useParams } from 'react-router-dom'; import type { ObterPessoaResponse } from '@/shared/api/types'; import { EntityDetailPage } from '@/shared/components/generic/EntityDetailPage'
export function ObterPessoaPage() { const { id } = useParams(); return <EntityDetailPage<ObterPessoaResponse & Record<string, unknown>> endpoint={`/pessoas/${id}`} queryKey="pessoas" backTo="/pessoas" editTo={`/pessoas/${id}/editar`} deleteEndpoint={`/pessoas/${id}`} titulo={x => x.pessoaNome} descricao={x => x.pessoaMiniBio} itens={x => [{ label: 'E-mail', valor: x.pessoaEmail }, { label: 'Telefone', valor: x.pessoaTelefone }, { label: 'CPF', valor: x.pessoaDocumento }, { label: 'Empresa', valor: x.pessoaEmpresa }, { label: 'Cargo', valor: x.pessoaCargo }, { label: 'Situação', valor: x.estaAtivo ? 'Ativo' : 'Inativo' }, { label: 'Criado em', valor: new Date(x.criadoEm).toLocaleString('pt-BR') }]} /> }
